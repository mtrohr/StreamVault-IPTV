package com.streamvault.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette ────────────────────────────────────────────────
val Primary = Color(0xFF6C63FF)
val PrimaryVariant = Color(0xFF8B83FF)
val PrimaryDark = Color(0xFF4A42DB)
val Secondary = Color(0xFF03DAC6)
val OnPrimary = Color(0xFFFFFFFF)

// ── Background & Surface ──────────────────────────────────────────
val Background = Color(0xFF0D0D0D)
val Surface = Color(0xFF1A1A1A)
val SurfaceElevated = Color(0xFF242424)
val SurfaceHighlight = Color(0xFF2D2D2D)
val SurfaceVariant = SurfaceElevated

// ── Text Colors ───────────────────────────────────────────────────
val OnBackground = Color(0xFFE8E8E8)
val OnSurface = Color(0xFFB0B0B0)
val OnSurfaceVariant = Color(0xFF888888)
val OnSurfaceDim = Color(0xFF666666)

// ── Status Colors ─────────────────────────────────────────────────
val ErrorColor = Color(0xFFFF6B6B)
val SuccessColor = Color(0xFF4ECB71)
val LiveIndicator = Color(0xFFFF4444)
val WarningColor = Color(0xFFFFB74D)

// ── Card & Focus ──────────────────────────────────────────────────
val FocusBorder = Primary
val CardBackground = Surface
val ProgressBar = Primary
val ProgressBarBackground = Color(0xFF333333)
