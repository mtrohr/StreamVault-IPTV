<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- IPTV providers often expose HTTP-only endpoints; keep behavior explicit. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
